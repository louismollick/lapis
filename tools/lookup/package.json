{
    "name": "lapis-lookup-tool",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "check": "biome check . && tsc --noEmit",
        "test": "npm run build && node --test dist/test/*.test.js",
        "fetch-dictionaries": "node dist/scripts/fetch-dictionaries.js",
        "prepare-database": "node dist/scripts/prepare-database.js"
    },
    "dependencies": {
        "better-sqlite3": "^11.10.0",
        "yomitan-core": "1.7.0"
    },
    "devDependencies": {
        "@biomejs/biome": "^1.9.4",
        "@types/better-sqlite3": "^7.6.13",
        "@types/node": "^22.10.2",
        "typescript": "^5.7.2"
    }
}
